{
  "name": "safari-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Safari Strives FastAPI backend",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 4000"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
